{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for WebRTC project",
  "main": "dist/index.js",
  "author": "Prince Narteh",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "nodemon .",
    "dev": "concurrently --kill-others \"yarn watch\" \"yarn start\""
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "tslint": "^6.1.3",
    "typescript": "^4.6.3"
  }
}
